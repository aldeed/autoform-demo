// Anything that might need to be adjusted easily can be centralized here.
// This is sent to clients, so don't put any secret stuff here.

AppConfig = {};

// Speed in milliseconds for automatic scrolling animations
AppConfig.scrollSpeed = 700;

// Easing function to use for automatic scrolling animations
AppConfig.scrollEasing = "easeInOutQuart";